请严格按照以下步骤执行需求文档标准化任务。这是非交互模式，直接产出结果，不要询问确认。

## 步骤

1. **读取模板**：打开 `$$TEMPLATE_PATH$$`，记住其章节结构作为输出格式。

2. **读取索引**：打开 `$$REQ_DIR$$/index.md`，获取 raw/ 目录下的源文件清单。

3. **读取源文件**：逐个打开 `$$REQ_DIR$$/raw/` 下的所有文本文件（.txt, .csv, .md 等），提取内容。

4. **分析提炼**：从源文件中提取功能点、验收标准、技术约束。若某项未提及，标注【待确认】。保持客观，不臆造。

5. **生成文档**：按照模板格式输出标准化需求文档，保存到 `$$REQ_DIR$$/final.md`。

6. **更新索引**：
   - 更新 `$$REQ_DIR$$/index.md`：状态改为"标准化完成"，最终文档部分添加 `- [final.md](final.md)`
   - 若 `$$WORKSPACE_ROOT$$/UPC_TEST/Claude/需求文档/index.md` 存在，将 `$$REQ_DIR$$` 对应需求加入"进行中"表格（如果尚未在表中）

## 重要

- 所有文件路径使用上述绝对路径
- raw/ 下若无文件，final.md 写"（无原始材料）"并正常完成
- **必须实际创建 final.md 文件**，不要只描述你应该做什么
- 执行完成后简要说明已完成的操作

开始执行。不要询问任何问题。
