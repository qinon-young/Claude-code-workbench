执行需求文档标准化。不要提问，直接操作。

第一步：用 Read 工具读取模板 `$$TEMPLATE_PATH$$`，了解输出格式。
第二步：用 Read 工具读取 `$$REQ_DIR$$/index.md`，获取原始文件清单。
第三步：用 Read 工具逐个读取 `$$REQ_DIR$$/raw/` 下的所有文件，提取内容。
第四步：从原始材料中提炼功能点、验收标准、技术约束。缺失的信息标注【待确认】。保持客观不臆造。
第五步：用 Write 工具创建 `$$REQ_DIR$$/final.md`，严格按模板格式生成标准化需求文档。
第六步：用 Edit 工具更新 `$$REQ_DIR$$/index.md`，将状态改为"标准化完成"，在最终文档部分添加 `- [final.md](final.md)`。

raw/ 下无文件时，final.md 写"（无原始材料）"并完成。所有路径使用上述绝对路径。完成后简要说明结果。
